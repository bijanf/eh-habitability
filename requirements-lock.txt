# Pinned environment for the frozen release (see PREREGISTRATION.md).
# Captured 2026-06-29. Pure CPU; no JAX/PyMC/Stan are required by the current code.
# Core runtime (eh_deeptime + eh_shallow):
numpy==2.4.2
scipy==1.17.1
matplotlib==3.10.8
pandas==2.3.3
# Real-data ingestion (eh_shallow/data.py, eh_deeptime/deeptime_data.py):
netCDF4==1.7.4
xarray==2026.2.0
requests==2.32.5
# Test:
pytest>=8.0
